# ==================== 前端（Web） ====================
# Vue3 源码（只在本地开发，不提交 Git）
web_frontend/

# Web 部署目录（构建产物提交 Git，服务器 git pull 获取）
# web/  ← 不忽略，需要提交

# ==================== 前端（AcWing） ====================
# Vue3 源码（只在本地开发，不提交 Git）
acapp_frontend/

# AcWing 构建项目（可选方案，本地保留）
acapp_build/

# AcWing 部署目录（构建产物提交 Git，服务器 git pull 获取）
# acapp/dist/  ← 不忽略，需要提交

# ==================== 后端（Backend） ====================
# Python
backend/__pycache__/
backend/**/__pycache__/
backend/*.pyc
backend/**/*.pyc
backend/*.pyo
backend/**/*.pyo

# Django
backend/db.sqlite3
backend/db.sqlite3-journal
backend/logs/
backend/*.log
backend/*.pid
backend/uwsgi.pid
backend/gunicorn.pid

# 虚拟环境
backend/venv/
backend/env/

# ==================== Android（adapp） ====================
# Android 源码（只在本地，服务器不需要）
adapp/*
# 但保留 README.md 说明文档
!adapp/README.md
# 保留产品报告和作业要求检查报告
!adapp/产品报告/
!adapp/产品报告/**
!adapp/作业要求检查报告.md

# Android 签名文件（敏感信息，不要提交）
adapp/*.keystore
adapp/keystore.properties
*.keystore
keystore.properties

# ==================== 文档 ====================
# docs/ 目录已移除 ignore，文档需要提交到 Git
# （之前错误地 ignore 了整个 docs/ 目录）

# ==================== 环境变量（敏感信息） ====================
.env
*.env
!.env.example
backend/ENV_PRODUCTION_READY_TO_COPY.txt

# 云端配置目录（包含敏感信息，仅本地使用）
cloud_settings/

# OAuth 凭证（敏感信息）
*CREDENTIALS_PRIVATE*
*_SECRET*
*_PRIVATE*

# 部署文档（可能包含敏感信息）
DEPLOY_COMMANDS.txt
QUICK_DEPLOY.txt
DEPLOY_WEATHER.sh
backend/DEPLOY_AI.sh
backend/DEPLOY_MANUAL.sh

# ==================== 通用 ====================
.DS_Store
*.swp
*.swo
*~
.vscode/
.idea/

# ==================== Nginx ====================
backend/nginx.conf
nginx.conf

